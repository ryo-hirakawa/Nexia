# 店舗売上管理ツール — Claude Code 作業ルール

## これは何をつくるか
コンサル会社が各クライアント（顧客の店舗）に提供する、売上・損益の管理 Web アプリ。
毎日の数字を入力 → 経営者用ダッシュボードに 日 / 週 / 月 で表示。店舗ごとにカスタマイズ。
最初のパイロットは「バー 1 店舗」。まず **コア 4 機能（M0′〜M3′）** で運用開始する。

- M0′ 基盤（このマイルストーン）: ログイン・テナント構造・RLS・隔離テスト
- M1′ 日次入力（コア）
- M2′ 計算 + 月初セットアップ（固定費の日割り・FL 率・営業利益）
- M3′ ダッシュボード（コア）

関連資料（Claude Artifacts）: 構想メモ / バー版ワイヤーフレーム / 開発ロードマップ / 実装プラン。

## 技術スタック（確定・勝手に変えない）
- Next.js 16（App Router）+ TypeScript … 画面とページの土台
- Supabase … データベース（数字の保管庫）/ ログイン / ファイル保存
- PostgreSQL（Supabase 内）… データ本体
- Vercel … 公開先（本番 + ステージング）
- Tailwind CSS v4 … 画面のスタイル（shadcn/ui は M1′ 以降で導入予定）
- Recharts … グラフ（M3′ で導入）
- テスト: Vitest

## 絶対に守るルール
1. **マルチテナント**: 会社ごとに分かれる全テーブルに `client_id` を持たせ、RLS（行レベルセキュリティ＝「この会社の人にはこの会社のデータしか見せない」をDBに守らせる仕組み）を有効にする。
2. **隔離テスト**: 「B社の利用者がA社のデータを取得すると 0 件になる」自動テスト（`tests/tenant-isolation.test.ts`）を維持し、**これが通らない変更はコミット/マージしない**。
3. **特権キー**（`SUPABASE_SERVICE_ROLE_KEY` = RLS を無視できる管理者キー）はサーバー側とテストのみ。`NEXT_PUBLIC_` を付けない。ブラウザ側に出さない。
4. **DB の構造変更**は必ず `supabase/migrations/` の SQL ファイル（変更履歴）で行う。本番 DB を直接いじらない。番号は連番（`0003_...sql`）。
5. **破壊的操作**（データ削除・本番反映・外部サービスへの送信）は実行前に人間へ確認する。
6. UI 文言はすべて日本語。金額は `¥1,234` 表記、数字は等幅（tabular-nums）。

## 進め方
- マイルストーンごとに、まずプランを出して人間の承認をもらう。
- 一区切りしたら、ステージング（確認用の別公開先）へデプロイし、人間がスマホで実機確認する。
- 各マイルストーンで `npm run test`・`npm run typecheck`・`npm run lint`・`/security-review` を実行し、要約を報告する。
- こまめにコミットする（1 コミット = 1 つの意味のある変更）。

## ディレクトリ
- `src/app/` … 画面（ルーティング）。`(app)/` グループはログイン必須エリア
- `src/lib/supabase/` … Supabase クライアント（browser / server / middleware）
- `src/lib/` … 認証ヘルパー、型、集計ロジック
- `supabase/migrations/` … DB 変更履歴（SQL）
- `tests/` … 隔離テストなど

## データモデル（M0′ 時点）
- `clients` … コンサルの顧客企業（テナント）
- `stores` … その企業の店舗（`client_id`、業種、テンプレ）
- `profiles` … 利用者。`auth.users` と 1:1（`id` 共有）。`is_platform_admin` = コンサル管理者フラグ
- `client_members` … 利用者 × 会社。`role`: `owner`（経営者）/ `manager`（店舗責任者）
- `store_members` … 利用者 × 店舗（店舗責任者が担当する店を絞る）

## 用語（このプロジェクトでの意味）
- クライアント / client = コンサルの顧客企業（テナント＝データを完全に分ける単位）
- 店舗 / store = その企業の店
- 管理者 = コンサル側（全クライアント横断で見られる）。`profiles.is_platform_admin = true`
- 経営者 = `client_members.role = 'owner'`（自社の全店舗）
- 店舗責任者 = `client_members.role = 'manager'`（`store_members` で割り当てられた店舗）
- 営業日 = 開店日ベース（深夜跨ぎは開店日に計上）※M1′ で実装
- FL 率 = (原価 + 人件費) ÷ 売上 ※M2′ で実装
